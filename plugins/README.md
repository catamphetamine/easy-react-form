Tracks abandoned forms (e.g. for Google Analytics). Is enabled by default.

## Use

```js
import { Form } from 'easy-react-form'
import OnAbandonPlugin from 'easy-react-form/plugins/on-abandon'

<Form plugins={[ OnAbandonPlugin ]}/>
```